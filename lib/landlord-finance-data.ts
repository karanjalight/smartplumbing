/**
 * Landlord portal finance — demo “logged-in” landlord until auth is wired.
 * Aligns tenant payments and payouts to one landlord record.
 */

/** Mock signed-in landlord (matches payouts ledger + tenant assignments). */
export const LANDLORD_PORTAL_LANDLORD_ID = "LND-001" as const;
